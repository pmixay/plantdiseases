# Developer-only dependencies (extends requirements.txt).
# Used for linting and running the test suite in CI.
-r requirements.txt
pytest>=8.0
# Starlette TestClient still passes `app=` to httpx.Client; httpx 0.28
# removed that parameter. Stay on the 0.27 series for compatibility with
# the pinned FastAPI 0.109 / Starlette stack.
httpx>=0.27,<0.28
ruff>=0.5
